# S1 handoff - 2026-09-06

## Stop state

S1-05B external machine acceptance is complete through authenticated deep-link
and fresh ChatGPT exact-Task readback. The cloud deployment is healthy and was
returned to the least-privilege state:

- Host: `workspace.ai-radar-lab.com` on Route 53 and a Lightsail static IPv4
- Public listener: Caddy on IPv4 TCP 443 only
- Application listeners: host loopback `127.0.0.1:3000-3001` only
- Deployed application image: `paw:9303de5`
- VM operational-source checkpoint: `4cb9015`
- Browser writes: disabled
- Identity bootstrap: disabled
- Generic Caddy service: disabled
- Dedicated `paw-web-ingress.service`: active at the final health check

Commits after `4cb9015` contain operational verification and documentation only;
they do not require an application image rebuild before the pending human test.

## Passed today

- Official Caddy `v2.11.4` installed and package-held.
- Route 53 preflight, writes-off and writes-on release checks passed.
- Port 80 remained closed; MCP, health, admin and application ports were not
  exposed through the public hostname.
- Google login linked the reviewed identity to the existing cloud Workspace;
  the bootstrap path was closed immediately afterward.
- Application restart, VM reboot, backup, SQLite integrity and current/rollback
  image recovery against an isolated database copy passed.
- The 90-second capacity window completed 30 samples and 60 concurrent MCP
  calls with zero HTTPS/MCP failures, zero restarts and no OOM event. Peak
  application memory was 58.99 MiB and minimum host available memory was 455 MiB.
- The permanently retained synthetic Task
  `976b64d1-a758-486a-a8aa-c0322e684ede` under Project
  `8568a3c3-768a-46b6-acf2-ac85e5108710` was completed once through Web.
- Completion verification returned `DONE`, record version 2, one audit row, one
  idempotency row and zero database changes on replay.
- A stale ChatGPT custom-app action snapshot was refreshed. A fresh conversation
  then called `workspace_get_task` and returned the exact status, version,
  completion time, Project ID and Task `webUrl` without conversation inference.
- Following that `webUrl` through Google login returned to the same completed
  Task page. UTC `2026-09-06T10:10:28.617Z` matched 20:10 Australia/Sydney.

Detailed evidence is in
[S1-05B.2 AI Radar Domain Ingress Results](S1_05B2_AI_RADAR_DOMAIN_INGRESS_RESULTS_v0.1.md).

## Pending gate

The S1 Web release is not fully accepted. Windows-PC-OFF iPhone Safari evidence
remains pending. The already-completed synthetic Task must not be mutated again,
and no real Task is authorized for testing.

OpenAI currently documents custom MCP apps as unavailable on mobile. Treat the
iPhone Safari direct-Web check and the already-passed ChatGPT Web MCP readback as
separate evidence. If strict G08 acceptance still requires a completion made on
iPhone, create a new synthetic fixture and obtain explicit authorization before
temporarily enabling writes.

## Resume sequence

1. Confirm the VM remains healthy and read-only:

   ```bash
   cd /opt/paw
   sudo ./deploy/cloud/web-ingress-health.sh
   sudo docker exec paw-paw-1 sh -c 'printf "WRITES=%s\nBOOTSTRAP=%s\n" "$PAW_WEB_WRITES_ENABLED" "$PAW_WEB_BOOTSTRAP_ENABLED"'
   ```

2. Confirm both values are `false`. Do not continue if health fails or either
   value is `true`.
3. Fully shut down the Windows PC. Sleep, hibernate or closing the laptop is not
   sufficient.
4. Disable iPhone Wi-Fi and use Safari over cellular data to open the retained
   synthetic Task URL. Complete Google login, verify `DONE`, reload, reopen the
   saved link, and navigate to its application and dashboard.
5. Record elapsed time, login repetitions, any confusing step and whether any
   action unexpectedly depended on the Windows PC.
6. Decide separately whether a second, explicitly authorized synthetic fixture
   is required for iPhone completion. Keep Web in read mode until that decision.

## Do not do

- Do not enable browser writes or identity bootstrap merely to perform the
  read-only iPhone check.
- Do not expose ports 80, 3000 or 3001 publicly.
- Do not recreate identity links, rotate credentials or display secret files.
- Do not rebuild or redeploy the application for these documentation-only
  commits.
- Do not delete the retained synthetic fixture or use a real Task as a test.
